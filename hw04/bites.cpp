#include <cstdio>
#include "bites"

int main(int argc, char** argv) {

	// open a file that has binary data
	FILE* fp = fopen("test.ppm", "r");
	
	int array[128];
	
	for(int=0; string[i] != '\0' && i<size; ++i)
	
		{
		fread(array, sizeof(int), 128, fp);
		
		}
		//return the pixel data in a one dimensional array of unsigned chars
		// plus the width and heighth of the image.
		fclose(fp);
	
	
	
	