#include <cstdio>
#include "powerup.h"

int main(int argc, char** argv) {
	
	//prints the result of the function which would raise the first number to the power of the second number
	printf("%d\n", powerup(4, 5));
}