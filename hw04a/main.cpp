#include <cstdio>
#include "umpteen2.h"

int main(int argv, char** argc) {

	unsigned char* bytes;
	int numBytes;
	int width, height;

	//umpteen2(bytes, numBytes);
	umpteen2(&width, &height);


}