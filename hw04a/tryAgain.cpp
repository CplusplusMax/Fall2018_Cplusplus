#include <cstdio>
#include "toy.h"

const char* string;

int main(int argc, char** argv) {
	
	int size = 8;
	printf("The length of the first line is: %d\n", checkLength(string, size));
	
}








/*
	
	int myArray[3];
	for(int i=0; i<3; ++i) {
		myArray[i]
	
	while(fscanf(fp, "%d%d", &value1, &value2) != "\0") {
		printf("%d%d", value1, value2);
	}
}
	//fread(myArray, sizeof(int), size, fp);   */